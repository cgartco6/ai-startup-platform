---

# 2 docker-compose.yml

```yaml
version: '3'

services:

  api:
    build: ./api
    ports:
      - "8000:8000"

  orchestrator:
    build: ./core/ai-orchestrator

  agents:
    build: ./agents

  vector-db:
    image: qdrant/qdrant
    ports:
      - "6333:6333"

  postgres:
    image: postgres
    environment:
      POSTGRES_PASSWORD: startup

  frontend:
    build: ./frontend/dashboard
    ports:
      - "3000:3000"
